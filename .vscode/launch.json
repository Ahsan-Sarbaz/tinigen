{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/${workspaceFolderBasename}.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            // "preLaunchTask": "Build"
        }
    ]
}